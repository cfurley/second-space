import userModel from "/src/models/userModel.js";
import validatePassword from "/src/controllers/userControllers.js";

// Response object mock
const createResMock = () => ({
  status: jest.fn().mockReturnThis(),
  json: jest.fn(),
});

describe("Username validation incorrect type int", () => {
    test("Valid: username was rejected", () => {

        username = 

  });
});
